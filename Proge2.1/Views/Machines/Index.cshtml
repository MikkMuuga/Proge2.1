@model IEnumerable<Proge2._1.Data.Machines>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Workers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supervision)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CostOfMachines)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Workers)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supervision)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CostOfMachines)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
